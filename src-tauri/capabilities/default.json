{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口与独立设置窗口能力；业务写入通过已登记命令进入 Rust。",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default"
  ]
}
