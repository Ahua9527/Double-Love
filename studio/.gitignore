node_modules/
dist/
release/

# Bundled release runtimes are prepared on the release machine only (prepare scripts + verify gate);
# keep the README contracts tracked but never the binaries/venvs themselves.
build/runtime/
!build/runtime/README.md
build/model-runtime/
!build/model-runtime/README.md

# Native module build artifacts (binding.gyp and src/ stay tracked).
build/native/
native/avfoundation-player/build/
